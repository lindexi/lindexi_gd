<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <Target Name="HainuhekallqelderJobehairlaw" AfterTargets="Build">
    <Warning Text="HebaleeloleafaNiberlallleardi $([MSBuild]::IsOSPlatform('Linux')) $([MSBuild]::IsOSPlatform('Windows'))" Condition="$([MSBuild]::IsOSPlatform('Linux'))"/>
  </Target>

</Project>
