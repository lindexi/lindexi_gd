<Page
    x:Class="rubbishProducer.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:rubbishProducer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBox Text="{x:Bind view.reminder}" TextWrapping="Wrap" Grid.Row="0" ScrollViewer.VerticalScrollBarVisibility="Visible"/>
        <Button Click="{x:Bind view.rubbish}" Grid.Row="1" Margin="10,10,10,10"/>
    </Grid>
</Page>
