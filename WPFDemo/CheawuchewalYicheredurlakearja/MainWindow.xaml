<Window x:Class="CheawuchewalYicheredurlakearja.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:CheawuchewalYicheredurlakearja.Views"
        mc:Ignorable="d"
        Title="PptxGenerator - SlideML Lab" Height="900" Width="1600"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="320"/>
        </Grid.ColumnDefinitions>

        <!-- 左侧边栏：Logo + 页面缩略图 -->
        <views:LeftSidebarPanel Grid.Column="0"/>

        <!-- 中间主内容区：标题栏 + 幻灯片预览 + XML编辑器 -->
        <views:MainContentPanel Grid.Column="1"/>

        <!-- 右侧 Copilot 面板 -->
        <views:CopilotPanel Grid.Column="2"/>
    </Grid>
</Window>
