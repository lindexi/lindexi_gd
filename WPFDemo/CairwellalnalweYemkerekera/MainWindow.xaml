<Window x:Class="CairwellalnalweYemkerekera.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CairwellalnalweYemkerekera"
        xmlns:system="clr-namespace:System;assembly=System.Runtime"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="elementStyle" TargetType="TextBlock">
            <Setter Property="TextDecorations" Value="Underline"></Setter>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Height" Value="30"></Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <StackPanel x:Name="MainStackPanel">
            <TextBlock Margin="30">
                <Hyperlink>HyperLink outside</Hyperlink>
            </TextBlock>
            <DataGrid IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTemplateColumn>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid>
                                    <TextBlock>
                                        <Run Text="text with "></Run>
                                        <Run FontWeight="Bold" Text="Strongger "></Run>
                                        <Run Text="content"></Run>
                                    </TextBlock>
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn ElementStyle="{StaticResource elementStyle}" Binding="{Binding}"></DataGridTextColumn>
                </DataGrid.Columns>
                <DataGrid.Items>
                    <system:String>Hyperlink in column</system:String>
                    <system:String>Hyperlink in column</system:String>
                </DataGrid.Items>
            </DataGrid>
        </StackPanel>
    </Grid>
</Window>
