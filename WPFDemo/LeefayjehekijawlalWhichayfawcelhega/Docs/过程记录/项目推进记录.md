# 项目推进记录

本文用于记录 AI 多页课件生成项目的推进过程。

## 2026-04-16

### 已完成事项 1：现有项目结构梳理

- 确认当前项目为 `net10.0-windows` 的 WPF 应用。
- 确认当前工程仅包含 `App` 与 `MainWindow` 空模板。

### 已完成事项 2：文档目录初始化

- 创建 `Docs/过程记录` 目录用于记录过程文件。
- 创建 `Docs/经验文档` 目录用于记录经验沉淀。
- 创建根级说明文档，明确文档分工。

### 已完成事项 3：WPF MVVM 经验文档输出

- 新增 `Docs/经验文档/WPF MVVM 最佳实践总结.md`。
- 总结 `View`、`ViewModel`、`Model`、`Service` 的职责边界。
- 明确当前项目应采用列表状态驱动、显式确认节点与单页错误回传的设计思路。

### 已完成事项 4：需求分析输出

- 新增 `Docs/经验文档/需求分析.md`。
- 明确使用 `ep-20260306101224-c8mtg` 生成逐页图片提示词。
- 明确使用 `ep-20260120102721-c4pxb` 生成每页 4 张候选图片。
- 明确图片生成前必须经过用户确认，且支持单页独立生成与重试。

### 已完成事项 5：项目框架设计输出

- 新增 `Docs/经验文档/项目框架设计.md`。
- 确定采用 `MainViewModel + SlidePageViewModel + Service` 的组织结构。
- 确定提示词生成、图片生成、页面拆分、导出分别由独立服务承载。

### 已完成事项 6：项目依赖补充

- 为项目新增 `CommunityToolkit.Mvvm` 依赖，用于构建 MVVM 基础能力。
- 为项目新增 `Microsoft.Agents.AI.OpenAI` 依赖，用于按既定方案接入豆包文本模型。

### 已完成事项 7：应用基础框架实现

- 新增 `Models`、`Services`、`ViewModels` 目录。
- 实现大纲拆分、提示词生成、图片生成、导出服务与页面状态模型。
- 在 `App.xaml.cs` 中完成应用启动组合与默认配置装配。

### 已完成事项 8：主界面布局实现

- 完成大纲输入区、导出目录输入区与全局状态区。
- 完成逐页列表布局，每页包含提示词编辑区、候选图片区与单页生成按钮。
- 完成底部导出按钮布局，并保留“用户确认后再生成图片”的交互约束。

### 已完成事项 9：代码审查与文档整理

- 新增 `Docs/过程记录/代码审查记录.md`。
- 更新 `Docs/README.md`，补全文档索引。
- 整理当前实现约束、后续优化方向与关键设计决定。

### 已完成事项 10：项目构建验证

- 执行项目构建，确认当前代码可以成功编译。
- 修正 `Agent` 构造方式与缺失命名空间引用问题。
- 验证当前结构已具备后续继续接入真实豆包接口调试的基础。
