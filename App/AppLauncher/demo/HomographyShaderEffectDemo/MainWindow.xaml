<Window x:Class="HomographyShaderEffectDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="WPF 单应变换像素着色器" Height="720" Width="1080"
        MinHeight="480" MinWidth="720"
        Background="#202124">
    <Window.Resources>
        <Style x:Key="CornerThumbStyle" TargetType="Thumb">
            <Setter Property="Width" Value="22" />
            <Setter Property="Height" Value="22" />
            <Setter Property="Cursor" Value="SizeAll" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Grid>
                            <Ellipse Fill="#F9AB00" Stroke="White" StrokeThickness="3" />
                            <Ellipse Margin="7" Fill="#202124" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Foreground="White" FontSize="24" FontWeight="SemiBold"
                       Text="Homography ShaderEffect" />
            <TextBlock Margin="0,6,0,0" Foreground="#BDC1C6" FontSize="14"
                       Text="拖动四个黄色控制点。CPU 更新逆单应矩阵，像素重采样由 GPU 完成。" />
        </StackPanel>

        <Border Grid.Row="2" Padding="20" Background="#303134" CornerRadius="8">
            <Viewbox Stretch="Uniform">
                <Grid x:Name="Viewport" Width="900" Height="560" ClipToBounds="True">
                    <Border>
                        <Border.Background>
                            <DrawingBrush TileMode="Tile" Viewport="0,0,24,24" ViewportUnits="Absolute">
                                <DrawingBrush.Drawing>
                                    <DrawingGroup>
                                        <GeometryDrawing Brush="#242424" Geometry="M0,0 H24 V24 H0 Z" />
                                        <GeometryDrawing Brush="#333333" Geometry="M0,0 H12 V12 H0 Z M12,12 H24 V24 H12 Z" />
                                    </DrawingGroup>
                                </DrawingBrush.Drawing>
                            </DrawingBrush>
                        </Border.Background>
                    </Border>

                    <Image x:Name="PreviewImage"
                           Source="/Image.png"
                           Stretch="Fill"
                           RenderOptions.BitmapScalingMode="HighQuality" />

                    <Canvas x:Name="AdornerCanvas" Background="Transparent">
                        <Polygon x:Name="Outline"
                                 Fill="Transparent"
                                 Stroke="#F9AB00"
                                 StrokeThickness="2"
                                 StrokeDashArray="5,3"
                                 IsHitTestVisible="False" />
                        <Thumb x:Name="TopLeftThumb" Tag="0" Style="{StaticResource CornerThumbStyle}"
                               DragDelta="CornerThumb_OnDragDelta" />
                        <Thumb x:Name="TopRightThumb" Tag="1" Style="{StaticResource CornerThumbStyle}"
                               DragDelta="CornerThumb_OnDragDelta" />
                        <Thumb x:Name="BottomRightThumb" Tag="2" Style="{StaticResource CornerThumbStyle}"
                               DragDelta="CornerThumb_OnDragDelta" />
                        <Thumb x:Name="BottomLeftThumb" Tag="3" Style="{StaticResource CornerThumbStyle}"
                               DragDelta="CornerThumb_OnDragDelta" />
                    </Canvas>
                </Grid>
            </Viewbox>
        </Border>
    </Grid>
</Window>
