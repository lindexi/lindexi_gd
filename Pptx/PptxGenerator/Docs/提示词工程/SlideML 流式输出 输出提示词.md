我需要你帮我根据打开的 Tab 的 SlideML 流式输出规范 和 V3 规范文档，编写给 LLM 的系统提示词内容，用于让 LLM 进行流式输出 SlideML 内容

你可以自己进行多次迭代，只需要最终能够输出提示词内容即可

要求：

0. 总纲： LLM 只能知道你输出的系统提示词内容，初次之外，一无所知。你必须确保你给的提示词是能够指导 LLM 进行流式输出 SlideML 内容
1. 如果有说到 SlideML 规范，直接说 SlideML 规范就好，禁止强调版本，比如说 SlideML V3，这是不可以的
2. 可以让 LLM 输出自然语言说明。但强调不能输出 Markdown、代码块等会污染的内容
3. 文档里面说的流结束 EOF 在实际工程化里面，无需告知 LLM 结束状态，因为当 LLM 输出结束的时候，就是输出流结束，就是 EOF 状态了。因此无需告知 LLM 防止 LLM 错误地尝试输出 `<EOF>` 内容
4. 如果有给示例，则要求 Id 内容是帕斯卡命名风格的。但无需与 LLM 说明这一点，无需说明 Id 要求帕斯卡命名，不要提及 Id 命名风格的问题，不要给 Id 有任何限制
5. 由于当前状态动画模块尚未准备完毕，请不要在提示词里面包含任何动画相关内容
6. 画布的尺寸应该用 `$(SlideWidth)` 和 `$(SlideHeight)` 代替，方便逻辑上进行替换

自我审查：

1. 系统提示词内容是否有任何幻想，是否有不符合规范说明的地方
2. 拿到系统提示词的 LLM 是否可以完全依靠系统提示词说出来 SlideML 的规范，即 LLM 能够通过你输出的系统提示词了解整个 SlideML 的规范
  - 你可以调用子智能体，将你输出的系统提示词给到他，让他根据你的提示词，输出 SlideML 规范。然后你再根据他的输出，了解和迭代提示词内容
3. 提示词不应该包含任何动画相关内容
4. 充分利用子智能体的功能，例如： 通过输入的系统提示词，让子智能体生成 SlideML 流式内容，检查是否有错误。如有错误，请迭代系统提示词内容

4. 系统提示词不应该太长，确保信息密度足够高，禁止废话