<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:system="clr-namespace:System;assembly=mscorlib">
    <SolidColorBrush x:Key="AnalysisPageBackgroundBrush" Color="#F4F8FA" />
    <SolidColorBrush x:Key="AnalysisSurfaceBrush" Color="#FFFFFF" />
    <SolidColorBrush x:Key="AnalysisNavyBrush" Color="#102C3A" />
    <SolidColorBrush x:Key="AnalysisBodyBrush" Color="#40545E" />
    <SolidColorBrush x:Key="AnalysisMutedBrush" Color="#778A93" />
    <SolidColorBrush x:Key="AnalysisTealBrush" Color="#13A89E" />
    <SolidColorBrush x:Key="AnalysisTealDarkBrush" Color="#0A7F79" />
    <SolidColorBrush x:Key="AnalysisTealLightBrush" Color="#E7F8F6" />
    <SolidColorBrush x:Key="AnalysisBorderBrush" Color="#DCE8EC" />
    <SolidColorBrush x:Key="AnalysisWarningBrush" Color="#E9A12F" />
    <SolidColorBrush x:Key="CoursewareThumbnailLetterboxBrush" Color="#111827" />
    <SolidColorBrush x:Key="CoursewareThumbnailBorderBrush" Color="#C8D8DE" />
    <system:Double x:Key="CoursewareThumbnailFrameWidth">180</system:Double>
    <system:Double x:Key="CoursewareWidescreenThumbnailFrameHeight">101.25</system:Double>
    <system:Double x:Key="CoursewareStandardThumbnailFrameHeight">135</system:Double>
    <CornerRadius x:Key="CoursewareThumbnailCornerRadius">6</CornerRadius>
    <Thickness x:Key="CoursewareThumbnailBorderThickness">1</Thickness>
    <Thickness x:Key="CoursewareThumbnailItemMargin">0,0,8,8</Thickness>

    <LinearGradientBrush x:Key="AnalysisHeroBackgroundBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#FFFFFF" Offset="0" />
        <GradientStop Color="#F1FAFB" Offset="0.55" />
        <GradientStop Color="#E8F8F5" Offset="1" />
    </LinearGradientBrush>

    <Style x:Key="AnalysisTitleTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource AnalysisNavyBrush}" />
        <Setter Property="FontSize" Value="30" />
        <Setter Property="FontWeight" Value="Bold" />
    </Style>

    <Style x:Key="AnalysisSectionTitleTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource AnalysisNavyBrush}" />
        <Setter Property="FontSize" Value="20" />
        <Setter Property="FontWeight" Value="Bold" />
    </Style>

    <Style x:Key="AnalysisCardTitleTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource AnalysisNavyBrush}" />
        <Setter Property="FontSize" Value="17" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="AnalysisBodyTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource AnalysisBodyBrush}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="LineHeight" Value="21" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <Style x:Key="AnalysisCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource AnalysisSurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource AnalysisBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="Padding" Value="16" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="14" Direction="270" ShadowDepth="2" Opacity="0.08" Color="#355361" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AnalysisPrimaryButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource AnalysisTealBrush}" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="22,11" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FocusVisualStyle">
            <Setter.Value>
                <Style TargetType="Control">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate>
                                <Border BorderBrush="#075E59" BorderThickness="2" CornerRadius="9" Margin="-3" />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            CornerRadius="7"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource AnalysisTealDarkBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.82" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AnalysisSecondaryButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="White" />
        <Setter Property="Foreground" Value="{StaticResource AnalysisNavyBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource AnalysisBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="16,9" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FocusVisualStyle">
            <Setter.Value>
                <Style TargetType="Control">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate>
                                <Border BorderBrush="{StaticResource AnalysisTealDarkBrush}" BorderThickness="2" CornerRadius="9" Margin="-3" />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="7"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="#F0FAF9" />
                            <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#8FD8D2" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="#E2F4F2" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AnalysisIconButtonStyle" TargetType="Button" BasedOn="{StaticResource AnalysisSecondaryButtonStyle}">
        <Setter Property="Padding" Value="10,7" />
        <Setter Property="FontWeight" Value="Normal" />
    </Style>

    <Style x:Key="AnalysisPillStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource AnalysisTealLightBrush}" />
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="Padding" Value="10,4" />
        <Setter Property="Margin" Value="0,0,8,0" />
    </Style>

    <Style x:Key="AnalysisTimelineItemStyle" TargetType="Border">
        <Setter Property="Background" Value="White" />
        <Setter Property="BorderBrush" Value="{StaticResource AnalysisBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="Padding" Value="12,11" />
        <Setter Property="Margin" Value="0,0,8,0" />
        <Setter Property="MinHeight" Value="84" />
    </Style>

    <Style x:Key="WorkspacePanelStyle" TargetType="Border">
        <Setter Property="Background" Value="White" />
        <Setter Property="BorderBrush" Value="{StaticResource AnalysisBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>
</ResourceDictionary>
