<Window x:Class="JecolairbuChejemdaina.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JecolairbuChejemdaina"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid x:Name="Grid">
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <local:FlipDrawingCanvas x:Name="FlipDrawingCanvas"/>

        <Grid Grid.Row="1">
            <Button Margin="10,10,10,10" Width="100" Content="清除" Click="Button_OnClick"></Button>
        </Grid>
    </Grid>
</Window>