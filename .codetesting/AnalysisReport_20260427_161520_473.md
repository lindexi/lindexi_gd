# Test Failures due possible code bugs

## LightTextEditorPlus.Skia.UnitTests.csproj - AppendText_ControlCharacters_DoesNotThrow
- **Confidence**: High
- **Test File**: LightTextEditorPlus\Tests\LightTextEditorPlus.Skia.UnitTests\API\SkiaTextEditor.EditTests.cs
- **Bug Location**: LightTextEditorPlus\LightTextEditorPlus.Core\Layout\LayoutUtils\LayoutChecker.cs@52

### Analysis
The test `AppendText_ControlCharacters_DoesNotThrow` expects that appending text containing control characters (\x01, \x02, \x03) should not throw an exception. However, the production code is throwing a TextEditorInnerDebugException in LayoutChecker.cs at line 52. The error message indicates that control characters have IsInvalidRunProperty=true but their CharDataInfo.Status is not set to CharDataInfoStatus.Undefined, which violates an internal consistency check. Since this is described as 'a previously passing test that became failing after recent changes', the production code's layout checking logic or control character handling was likely changed recently. Control characters are valid Unicode characters that text editors need to handle gracefully. The fix needs to be in the production code to either: (1) filter control characters before layout, (2) ensure control characters get CharDataInfo.Status=Undefined when they have IsInvalidRunProperty, or (3) not mark control characters as IsInvalidRunProperty if they can be laid out. The test itself is correctly written and its expectation is reasonable.

### Suggested Fix
The issue is in LightTextEditorPlus.Core\Layout\LayoutUtils\LayoutChecker.cs at line 44-55. The check assumes that any character with `IsInvalidRunProperty` must have `CharDataInfo.Status == CharDataInfoStatus.Undefined`. However, control characters (like \x01, \x02, \x03) are being marked as `IsInvalidRunProperty` but are getting a different status during layout.

Possible fixes:

1. **Filter control characters during text input** - Before the text reaches the layout stage, filter out or replace control characters in the AppendText/DocumentManager code path.

2. **Fix the character property assignment** - Ensure that when control characters are processed, if they get `IsInvalidRunProperty=true`, they must also get `CharDataInfo.Status=CharDataInfoStatus.Undefined`.

3. **Relax the debug check** - If control characters are intentionally being processed with `IsInvalidRunProperty` but with a valid layout status, the debug assertion in LayoutChecker.cs needs to account for this case.

The most likely fix location is in the character processing/layout code where `CharDataInfo.Status` is assigned, ensuring control characters get the proper status value that matches their `IsInvalidRunProperty` state. Alternatively, control characters should be filtered in `TextEditorCore.AppendText` or `DocumentManager.AppendText` before they reach the layout system.

## LightTextEditorPlus.Skia.UnitTests.csproj - EditAndReplaceRun_WithNegativeLengthSelection_ExecutesSuccessfully
- **Confidence**: High
- **Test File**: LightTextEditorPlus\Tests\LightTextEditorPlus.Skia.UnitTests\API\SkiaTextEditor.EditTests.cs
- **Bug Location**: LightTextEditorPlus\LightTextEditorPlus.Core\Carets\Selection.cs@17

### Analysis
The test is correct in expecting that Selection should handle negative lengths appropriately. The bug is in the Selection constructor at line 17-24. When a negative length is provided (e.g., new Selection(new CaretOffset(5), -3)), the constructor creates StartOffset=5 and EndOffset=2, but then incorrectly sets FrontOffset=StartOffset=5 and BehindOffset=EndOffset=2. This violates the invariant that FrontOffset <= BehindOffset. The Selection.Length property (line 63) is defined as 'BehindOffset.Offset - FrontOffset.Offset', which would give -3 in this case. The DocumentManager.ReplaceCore method at line 934 throws SelectionOutOfRangeException because it checks if BehindOffset (2) > CharCount (0), which is true. The constructor should swap FrontOffset and BehindOffset when length is negative, similar to how the Selection(CaretOffset, CaretOffset) constructor (line 32-47) handles the case where startOffset > endOffset.

### Suggested Fix
The bug is in the Selection constructor at C:\lindexi\Code\lindexi_gd\LightTextEditorPlus\LightTextEditorPlus\LightTextEditorPlus.Core\Carets\Selection.cs line 17-24. The constructor should handle negative lengths by ensuring FrontOffset and BehindOffset maintain the invariant that FrontOffset <= BehindOffset. The fix should be:

```csharp
[DebuggerStepThrough]
public Selection(CaretOffset startOffset, int length)
{
    StartOffset = startOffset;
    EndOffset = new CaretOffset(startOffset.Offset + length);

    if (length >= 0)
    {
        FrontOffset = startOffset;
        BehindOffset = EndOffset;
    }
    else
    {
        FrontOffset = EndOffset;
        BehindOffset = startOffset;
    }
}
```

This ensures that when length is negative (e.g., -3), FrontOffset becomes the smaller value (2) and BehindOffset becomes the larger value (5), maintaining the invariant that FrontOffset <= BehindOffset, just like the other constructor does.

## LightTextEditorPlus.Skia.UnitTests.csproj - EditAndReplaceRun_WithNullTextRun_ThrowsException
- **Confidence**: High
- **Test File**: LightTextEditorPlus\Tests\LightTextEditorPlus.Skia.UnitTests\API\SkiaTextEditor.EditTests.cs
- **Bug Location**: LightTextEditorPlus\LightTextEditorPlus.Skia\API\SkiaTextEditor.Edit.cs@31

### Analysis
The test correctly expects that calling EditAndReplaceRun with a null textRun should throw ArgumentNullException. The method signature in SkiaTextEditor.EditAndReplaceRun declares 'SkiaTextRun textRun' as non-nullable, but the implementation doesn't validate this parameter. The method simply forwards the call to TextEditorCore.EditAndReplaceRun which accepts nullable IImmutableRun? and doesn't throw an exception for null values. Since this was a previously passing test, the validation logic was likely removed in recent changes. The production code should validate the non-nullable parameter and throw ArgumentNullException when null is passed.

### Suggested Fix
The SkiaTextEditor.EditAndReplaceRun method in file 'C:\lindexi\Code\lindexi_gd\LightTextEditorPlus\LightTextEditorPlus\LightTextEditorPlus.Skia\API\SkiaTextEditor.Edit.cs' needs to validate the textRun parameter. Add a null check at the beginning of the method:

```csharp
public void EditAndReplaceRun(SkiaTextRun textRun, Selection? selection = null)
{
    if (textRun == null)
    {
        throw new ArgumentNullException(nameof(textRun));
    }
    TextEditorCore.EditAndReplaceRun(textRun, selection);
}
```

This matches the API contract indicated by the non-nullable parameter type and ensures the method throws ArgumentNullException as expected by the test.

## LightTextEditorPlus.Skia.UnitTests.csproj - BuildDecoration_ZeroHeightBounds_ReturnsValidResult
- **Confidence**: High
- **Test File**: LightTextEditorPlus\Tests\LightTextEditorPlus.Skia.UnitTests\Document\Property_\Decorations\WaveLineTextEditorDecorationTests.cs
- **Bug Location**: LightTextEditorPlus\LightTextEditorPlus.Skia\Document\Property_\Decorations\WaveLineTextEditorDecoration.cs@86

### Analysis
The test provides bounds with zero height, which causes the production code to calculate WaveLength = 0 (bounds.Height * 2). In DrawWaveLine at line 86, this leads to division by zero when calculating howManyWaves = distance / waveLength, resulting in Infinity or NaN. The loop at line 100 then attempts to iterate an infinite number of times, causing the List to grow until OutOfMemoryException is thrown at line 111. The production code needs to handle zero or invalid dimensions gracefully by either early-returning, using a minimum value, or checking for invalid dimensions before performing calculations.

### Suggested Fix
The WaveLineTextEditorDecoration.BuildDecoration method needs to validate bounds dimensions before calling DrawWaveLine. At line 24-48 in WaveLineTextEditorDecoration.cs, add a check after line 31:

```csharp
var bounds = argument.RecommendedBounds;
if (bounds.Height <= 0 || bounds.Width <= 0)
{
    // Skip rendering for invalid bounds
    return new BuildDecorationResult()
    {
        TakeCharCount = argument.CharDataList.Count,
    };
}
var waveLine = new WaveLine()
```

Alternatively, the DrawWaveLine method should validate waveLength before division at line 86:

```csharp
var waveLength = WaveLength;
if (waveLength <= 0)
{
    return; // Cannot draw wave with zero or negative wave length
}
var waveHeight = WaveHeight;
var howManyWaves = distance / waveLength;
```

## LightTextEditorPlus.Skia.UnitTests.csproj - SetCharDataInfo_GlyphClusterExceedsTotalLength_ClampsToValidRange
- **Confidence**: High
- **Test File**: LightTextEditorPlus\Tests\LightTextEditorPlus.Skia.UnitTests\Platform\Utils\CharRenderInfoSetterTests.cs
- **Bug Location**: LightTextEditorPlus\LightTextEditorPlus.Skia\Platform\Utils\CharRenderInfoSetter.cs@82

### Analysis
The test is explicitly designed to verify that the production code handles GlyphCluster values exceeding totalUtf16Length gracefully by clamping them. The test sets GlyphCluster=999 with a totalUtf16Length of 1 (single character 'A'). The production code at line 82 has Debug.Assert(glyphCluster <= totalUtf16Length) which fails in this scenario, even though line 83 contains logic to clamp the value: 'glyphCluster > totalUtf16Length ? (uint) totalUtf16Length : glyphCluster'. The Debug.Assert contradicts the graceful handling that the code is supposed to provide and should be removed.

### Suggested Fix
Remove the Debug.Assert at line 82 in CharRenderInfoSetter.cs. The assertion prevents the graceful clamping behavior that is implemented on line 83. The code already handles out-of-range GlyphCluster values correctly by clamping them to totalUtf16Length.

Change:
```csharp
uint glyphCluster = charSizeInfoSpan[i].GlyphCluster;
Debug.Assert(glyphCluster <= totalUtf16Length);
logicalClusterSpan[logicalClusterCount] = glyphCluster > totalUtf16Length ? (uint) totalUtf16Length : glyphCluster;
```

To:
```csharp
uint glyphCluster = charSizeInfoSpan[i].GlyphCluster;
logicalClusterSpan[logicalClusterCount] = glyphCluster > totalUtf16Length ? (uint) totalUtf16Length : glyphCluster;
```

## LightTextEditorPlus.Skia.UnitTests.csproj - Constructor_WithAbsoluteUnitAndXIsNaN_ThrowsArgumentOutOfRangeException
- **Confidence**: High
- **Test File**: LightTextEditorPlus\Tests\LightTextEditorPlus.Skia.UnitTests\Primitive\Brushes\GradientSkiaTextBrushRelativePointTests.cs
- **Bug Location**: LightTextEditorPlus\LightTextEditorPlus.Skia\Primitive\Brushes\GradientSkiaTextBrushRelativePoint.cs@28

### Analysis
The test correctly expects that NaN should be rejected when using Absolute unit with [0,1] range validation. The production code bug is on line 28 and 33: the pattern matching `x is < 0 or > 1` does not match NaN values because NaN comparisons always return false in C#. The validation needs to explicitly check for NaN values using float.IsNaN(). There's also a semantic conflict: an existing test at line 122-134 (ToSKPoint_AbsoluteUnitWithNaNCoordinates_ReturnsNaNResult) successfully creates an instance with NaN and Absolute, expecting it to work. However, based on the constructor's XML documentation and the validation logic intent (coordinates must be in [0,1] for Absolute unit), the failing test expectation appears correct - NaN should be rejected. The production code needs to add explicit NaN checks.

### Suggested Fix
In GradientSkiaTextBrushRelativePoint.cs, lines 28 and 33, the validation logic needs to explicitly check for NaN values. Change:

```csharp
if (x is < 0 or > 1)
```

to:

```csharp
if (float.IsNaN(x) || x is < 0 or > 1)
```

And change:

```csharp
if (y is < 0 or > 1)
```

to:

```csharp
if (float.IsNaN(y) || y is < 0 or > 1)
```

Note: There's also a conflicting test at line 122-134 (ToSKPoint_AbsoluteUnitWithNaNCoordinates_ReturnsNaNResult) that creates an Absolute unit point with NaN coordinates without expecting an exception. This test should probably be updated or removed to align with the validation requirements.

## LightTextEditorPlus.Skia.UnitTests.csproj - ToSKPoint_AbsoluteUnit_ReturnsExactCoordinates
- **Confidence**: High
- **Test File**: LightTextEditorPlus\Tests\LightTextEditorPlus.Skia.UnitTests\Primitive\Brushes\GradientSkiaTextBrushRelativePointTests.cs
- **Bug Location**: LightTextEditorPlus\LightTextEditorPlus.Skia\Primitive\Brushes\GradientSkiaTextBrushRelativePoint.cs@26

### Analysis
The constructor of GradientSkiaTextBrushRelativePoint has inverted logic. On line 26, it checks `if (unit != RelativeUnit.Relative)` and then validates that coordinates are in [0, 1] range. This means when unit is Absolute, it enforces the [0, 1] constraint, which is incorrect. The constraint should only apply when unit IS Relative. The test correctly expects that when using RelativeUnit.Absolute, any coordinate values (including 3.14159, 50, 1000, etc.) should be accepted without validation, as absolute coordinates are not bounded to [0, 1]. The condition should be `if (unit == RelativeUnit.Relative)` instead of `if (unit != RelativeUnit.Relative)`.

### Suggested Fix
In the file `C:\lindexi\Code\lindexi_gd\LightTextEditorPlus\LightTextEditorPlus\LightTextEditorPlus.Skia\Primitive\Brushes\GradientSkiaTextBrushRelativePoint.cs`, line 26 needs to be changed from:

```csharp
if (unit != RelativeUnit.Relative)
```

to:

```csharp
if (unit == RelativeUnit.Relative)
```

This will ensure that the [0, 1] range validation only applies when using Relative units, and Absolute units can accept any coordinate value.

## LightTextEditorPlus.Skia.UnitTests.csproj - ToSKPoint_AbsoluteUnitWithExtremeValues_ReturnsExactValues
- **Confidence**: High
- **Test File**: LightTextEditorPlus\Tests\LightTextEditorPlus.Skia.UnitTests\Primitive\Brushes\GradientSkiaTextBrushRelativePointTests.cs
- **Bug Location**: LightTextEditorPlus\LightTextEditorPlus.Skia\Primitive\Brushes\GradientSkiaTextBrushRelativePoint.cs@26

### Analysis
The constructor validation logic in GradientSkiaTextBrushRelativePoint is inverted. The condition 'if (unit != RelativeUnit.Relative)' should be 'if (unit == RelativeUnit.Relative)'. Currently, when Absolute mode is used, it incorrectly validates the [0,1] range, and when Relative mode is used, it skips validation. This is backwards. The exception message itself confirms the intent: '当 RelativeUnit 为 Relative 时，坐标值必须在 [0, 1] 范围内' (When RelativeUnit is Relative, coordinate values must be in [0, 1] range). The test is correct - it expects Absolute mode to accept extreme float values without validation.

### Suggested Fix
In GradientSkiaTextBrushRelativePoint.cs at line 26, change the condition from `if (unit != RelativeUnit.Relative)` to `if (unit == RelativeUnit.Relative)`. The corrected constructor validation should be:

```csharp
if (unit == RelativeUnit.Relative)
{
    if (x is < 0 or > 1)
    {
        throw new ArgumentOutOfRangeException(nameof(x), x, ExceptionMessages.GradientSkiaTextBrushRelativePoint_RelativeCoordinateMustBeInRange);
    }

    if (y is < 0 or > 1)
    {
        throw new ArgumentOutOfRangeException(nameof(y), y, ExceptionMessages.GradientSkiaTextBrushRelativePoint_RelativeCoordinateMustBeInRange);
    }
}
```

This ensures that the [0,1] range validation only applies when using Relative mode, and Absolute mode can accept any float values including extreme values like float.MaxValue and float.MinValue.

## LightTextEditorPlus.Skia.UnitTests.csproj - ToSKPoint_AbsoluteUnitWithNegativeInfinity_ReturnsInfinityResult
- **Confidence**: High
- **Test File**: LightTextEditorPlus\Tests\LightTextEditorPlus.Skia.UnitTests\Primitive\Brushes\GradientSkiaTextBrushRelativePointTests.cs
- **Bug Location**: LightTextEditorPlus\LightTextEditorPlus.Skia\Primitive\Brushes\GradientSkiaTextBrushRelativePoint.cs@26

### Analysis
The test correctly expects that when using RelativeUnit.Absolute, negative infinity values should be allowed for coordinates. However, the production code has inverted validation logic in the constructor. At line 26 of GradientSkiaTextBrushRelativePoint.cs, the condition 'if (unit != RelativeUnit.Relative)' is backwards. This causes the [0,1] range validation to be applied when unit is Absolute (not Relative), and skips validation when unit is Relative. The logic should be inverted: Relative coordinates need [0,1] validation, while Absolute coordinates can accept any float value including infinities. The error message in the exception also contradicts the actual logic, stating 'When RelativeUnit is Relative' when it actually throws for non-Relative units.

### Suggested Fix
In GradientSkiaTextBrushRelativePoint.cs at line 26, change the condition from:
```csharp
if (unit != RelativeUnit.Relative)
```
to:
```csharp
if (unit == RelativeUnit.Relative)
```

This will ensure that:
- When unit is Relative, coordinates must be in [0, 1] range (as intended)
- When unit is Absolute, coordinates can be any float value including infinities (as the test expects)

The corrected validation block should be:
```csharp
if (unit == RelativeUnit.Relative)
{
    if (x is < 0 or > 1)
    {
        throw new ArgumentOutOfRangeException(nameof(x), x, ExceptionMessages.GradientSkiaTextBrushRelativePoint_RelativeCoordinateMustBeInRange);
    }

    if (y is < 0 or > 1)
    {
        throw new ArgumentOutOfRangeException(nameof(y), y, ExceptionMessages.GradientSkiaTextBrushRelativePoint_RelativeCoordinateMustBeInRange);
    }
}
```

## LightTextEditorPlus.Skia.UnitTests.csproj - ToSKPoint_AbsoluteUnitWithPositiveInfinity_ReturnsInfinityResult
- **Confidence**: High
- **Test File**: LightTextEditorPlus\Tests\LightTextEditorPlus.Skia.UnitTests\Primitive\Brushes\GradientSkiaTextBrushRelativePointTests.cs
- **Bug Location**: LightTextEditorPlus\LightTextEditorPlus.Skia\Primitive\Brushes\GradientSkiaTextBrushRelativePoint.cs@26

### Analysis
The production code has a logical error in the constructor at line 26. The condition `if (unit != RelativeUnit.Relative)` is backwards. It should be `if (unit == RelativeUnit.Relative)` instead. Currently, it validates the [0,1] range when using Absolute coordinates, but the validation should only apply to Relative coordinates. When using RelativeUnit.Relative, coordinates represent percentages (0-1 range). When using RelativeUnit.Absolute, coordinates are absolute pixel positions and should accept any float value including infinity. The test correctly expects that creating a point with PositiveInfinity using Absolute unit should succeed, but the inverted logic causes it to fail.

### Suggested Fix
In the file `GradientSkiaTextBrushRelativePoint.cs` at line 26, change the condition from `if (unit != RelativeUnit.Relative)` to `if (unit == RelativeUnit.Relative)`. The corrected code should be:

```csharp
if (unit == RelativeUnit.Relative)
{
    if (x is < 0 or > 1)
    {
        throw new ArgumentOutOfRangeException(nameof(x), x, ExceptionMessages.GradientSkiaTextBrushRelativePoint_RelativeCoordinateMustBeInRange);
    }

    if (y is < 0 or > 1)
    {
        throw new ArgumentOutOfRangeException(nameof(y), y, ExceptionMessages.GradientSkiaTextBrushRelativePoint_RelativeCoordinateMustBeInRange);
    }
}
```

This ensures that range validation only applies to Relative coordinates (where values must be between 0 and 1 as percentages), while Absolute coordinates can have any value including infinity.

## LightTextEditorPlus.Skia.UnitTests.csproj - Apply_SpecialDoubleOpacityValues_NoAlphaAdjustment
- **Confidence**: High
- **Test File**: LightTextEditorPlus\Tests\LightTextEditorPlus.Skia.UnitTests\Primitive\Brushes\SolidColorSkiaTextBrushTests.cs
- **Bug Location**: LightTextEditorPlus\LightTextEditorPlus.Skia\Primitive\Brushes\SolidColorSkiaTextBrush.cs@20

### Analysis
The test fails because the production code condition `if (context.Opacity < 1)` incorrectly evaluates to `true` for `double.NegativeInfinity`. In C#, `double.NegativeInfinity < 1` returns `true`, causing the alpha adjustment logic to execute. When alpha (180) is multiplied by negative infinity and cast to byte, it becomes 0. The test correctly expects that special double values (NaN, PositiveInfinity, NegativeInfinity) should not trigger alpha adjustment. The production code should validate that opacity is a finite value in the valid range [0, 1) before applying the adjustment.

### Suggested Fix
In SolidColorSkiaTextBrush.cs at line 20, change the condition from `if (context.Opacity < 1)` to `if (context.Opacity is >= 0 and < 1)` or add a check for finite values like `if (double.IsFinite(context.Opacity) && context.Opacity >= 0 && context.Opacity < 1)`. This ensures that only valid opacity values in the range [0, 1) trigger alpha adjustment, and special double values (NaN, infinities) are safely ignored.

Fixed code:
```csharp
if (context.Opacity is >= 0 and < 1)
{
    // 处理透明度
    skPaint.Color = skPaint.Color.WithAlpha((byte) (skPaint.Color.Alpha * context.Opacity));
}
```

Alternatively, for more explicit handling:
```csharp
if (double.IsFinite(context.Opacity) && context.Opacity >= 0 && context.Opacity < 1)
{
    // 处理透明度
    skPaint.Color = skPaint.Color.WithAlpha((byte) (skPaint.Color.Alpha * context.Opacity));
}
```

## LightTextEditorPlus.Skia.UnitTests.csproj - GetHandwritingPaperInfo_ValidCharData_ReturnsCorrectInfo
- **Confidence**: High
- **Test File**: LightTextEditorPlus\Tests\LightTextEditorPlus.Skia.UnitTests\Rendering\RenderManagerExtensionTests.cs
- **Bug Location**: LightTextEditorPlus.Core/Properties/AssemblyInfo.cs@7

### Analysis
The test is skipped because it cannot construct RenderInfoProvider and ParagraphLineRenderInfo objects which have internal constructors in the Core assembly. These types are not interfaces and cannot be mocked with Moq. The test is in LightTextEditorPlus.Skia.UnitTests, but the required types are in LightTextEditorPlus.Core with internal constructors. Following the pattern seen in the codebase where LightTextEditorPlus.Core exposes internals to LightTextEditorPlus.Core.Tests and LightTextEditorPlus.Wpf exposes internals to LightTextEditorPlus.Tests, the Core assembly should expose its internals to LightTextEditorPlus.Skia.UnitTests to enable testing of the Skia extensions that depend on Core types.

### Suggested Fix
Add the following line to `LightTextEditorPlus.Core/Properties/AssemblyInfo.cs` after the existing InternalsVisibleTo attributes:

```csharp
[assembly: InternalsVisibleTo("LightTextEditorPlus.Skia.UnitTests")]
```

This will allow the Skia unit tests to access internal constructors and types from the Core assembly, enabling proper testing of Skia extension methods like GetHandwritingPaperInfo that depend on Core types with internal constructors. After adding this attribute, the test can be rewritten to construct the necessary test objects.

## LightTextEditorPlus.Skia.UnitTests.csproj - GetHandwritingPaperInfo_VariousFontMetrics_CalculatesCorrectly
- **Confidence**: High
- **Test File**: LightTextEditorPlus\Tests\LightTextEditorPlus.Skia.UnitTests\Rendering\RenderManagerExtensionTests.cs
- **Bug Location**: Production assembly containing RenderInfoProvider and ParagraphLineRenderInfo classes

### Analysis
This test is skipped because the production code uses internal constructors for RenderInfoProvider and ParagraphLineRenderInfo, making them impossible to instantiate from the test assembly. The test comments (lines 36-43, 67-71, 90-96, 114-128) explicitly document this architectural issue. All 4 tests in this test class have the same problem. The test is not actually failing - it's intentionally skipped because the necessary test infrastructure doesn't exist in the production code. This is a design issue in the production code where testability was not considered. The test itself is well-designed and documents the expected behavior clearly, including edge cases for font metrics (zero values, negative ascent, positive descent) and the calculation formulas (lines 123-128). To fix this, the production code needs to provide one of: (1) Factory methods or test helpers to create these objects, (2) Public constructors, or (3) InternalsVisibleTo attribute to expose internals to the test assembly.

### Suggested Fix
To make these tests runnable, the production code needs architectural changes to improve testability:

**Option 1: Add InternalsVisibleTo attribute** (Recommended)
In the production assembly that contains RenderInfoProvider and ParagraphLineRenderInfo, add to AssemblyInfo.cs or a .cs file:
```csharp
[assembly: InternalsVisibleTo("LightTextEditorPlus.Skia.UnitTests")]
```

**Option 2: Create public factory methods**
Add factory methods to create test instances:
```csharp
public static class TestHelpers
{
    public static RenderInfoProvider CreateTestRenderInfoProvider(/* parameters */)
    {
        // Create and return test instance
    }
    
    public static ParagraphLineRenderInfo CreateTestParagraphLineRenderInfo(/* parameters */)
    {
        // Create and return test instance
    }
}
```

**Option 3: Make constructors public** (Least recommended)
Change the constructors from internal to public, though this may violate encapsulation principles.

The recommended approach is Option 1 (InternalsVisibleTo) as it maintains the internal access level while allowing the test assembly to access these types.

## LightTextEditorPlus.Skia.UnitTests.csproj - BuildCaretAndSelectionRender_NullRenderInfoProvider_ThrowsArgumentNullException
- **Confidence**: High
- **Test File**: LightTextEditorPlus\Tests\LightTextEditorPlus.Skia.UnitTests\SkiaTextEditorTests.cs
- **Bug Location**: LightTextEditorPlus\LightTextEditorPlus.Skia\Rendering\RenderManager.cs@43-45

### Analysis
The test correctly expects an ArgumentNullException when BuildCaretAndSelectionRender is called with a null renderInfoProvider. However, the production code in RenderManager.BuildCaretAndSelectionRender (line 43-70 in RenderManager.cs) does not validate the renderInfoProvider parameter before using it. When selection.IsEmpty is true (which it is in the test with selection having length 0), the method immediately calls renderInfoProvider.GetCurrentCaretRenderInfo() on line 49 without checking if renderInfoProvider is null, causing a NullReferenceException. The same issue exists on line 66 for the else branch where renderInfoProvider.GetSelectionBoundsList is called. The production code should add a null check at the beginning of the method and throw ArgumentNullException when renderInfoProvider is null.

### Suggested Fix
The RenderManager.BuildCaretAndSelectionRender method needs to add parameter validation at the beginning of the method. Add the following code at line 45 (right after the method signature and opening brace):

```csharp
if (renderInfoProvider is null)
{
    throw new ArgumentNullException(nameof(renderInfoProvider));
}
```

This will ensure that when the method is called with a null renderInfoProvider, it throws an ArgumentNullException as expected by the test, rather than allowing the code to proceed and throw a NullReferenceException when trying to call methods on the null reference.

## LightTextEditorPlus.Skia.UnitTests.csproj - GetCurrentTextRender_WhenCalledMultipleTimes_ReturnsConsistentResult
- **Confidence**: High
- **Test File**: LightTextEditorPlus\Tests\LightTextEditorPlus.Skia.UnitTests\SkiaTextEditorTests.cs
- **Bug Location**: LightTextEditorPlus\LightTextEditorPlus.Skia\Rendering\RenderManager.cs@105

### Analysis
The Debug.Assert at line 105 in RenderManager.cs has an incorrect assumption. It asserts that TextEditor.TextEditorCore.IsDirty must be false when GetCurrentTextRender() is called for the first time (_currentRender is null). However, when a SkiaTextEditor is newly created and GetCurrentTextRender() is called immediately without any prior layout or rendering, the TextEditorCore.IsDirty property will be true because no layout has been performed yet. The code at lines 106-107 handles this by calling GetRenderInfo() and Render(), which will perform the necessary layout. The assertion is overly strict and incorrect for the initialization case. The assertion should either be removed, or the initialization logic should ensure IsDirty is false before this method can be called.

### Suggested Fix
In `LightTextEditorPlus.Skia\Rendering\RenderManager.cs` at line 105, remove or modify the Debug.Assert. The current assertion `Debug.Assert(!TextEditor.TextEditorCore.IsDirty);` is incorrect because when _currentRender is null (first call), the TextEditorCore will be dirty for a newly created text editor. The subsequent code (lines 106-107) properly handles this by getting render info and calling Render. 

Recommended fix:
```csharp
public ITextEditorContentSkiaRenderer GetCurrentTextRender()
{
    if (_currentRender is null)
    {
        // 首次渲染,需要尝试获取一下
        // Remove the incorrect assertion: Debug.Assert(!TextEditor.TextEditorCore.IsDirty);
        RenderInfoProvider renderInfoProvider = TextEditor.TextEditorCore.GetRenderInfo();
        Render(renderInfoProvider);
    }

    Debug.Assert(!_currentRender.IsDisposed);

    _currentRender.IsUsed = true;
    return _currentRender;
}
```

Alternatively, if the assertion is intended to catch an edge case, it should be conditional:
```csharp
// Only assert if we've done any layout before
Debug.Assert(!_isAnyLayoutUpdate || !TextEditor.TextEditorCore.IsDirty);
```

