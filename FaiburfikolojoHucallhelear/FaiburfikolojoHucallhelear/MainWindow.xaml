<Window x:Class="FaiburfikolojoHucallhelear.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FaiburfikolojoHucallhelear"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        x:Name="Root">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <ListBox ItemsSource="{Binding ElementName=Root,Path=List}" VirtualizingPanel.ScrollUnit="Pixel" VirtualizingPanel.IsVirtualizing="False">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Margin="10,10,10,10" Width="100" Height="100" Background="#565656">
                        <TextBlock FontSize="30" Text="{Binding}"></TextBlock>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Button HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="10,10,10,10" Click="ButtonBase_OnClick">确定</Button>
    </Grid>
</Window>
