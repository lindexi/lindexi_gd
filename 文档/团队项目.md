#团队开发

github：https://github.com/lindexi/tgrfdw

进入个人新建的一个目录git clone https://github.com/lindexi/tgrfdw

git remote remove origin

git remote add origin git@github.com:lindexi/tgrfdw

新建分支

git branch 自己名字

git checkout 自己名字

不要上传代码到master

如果没有个人密钥

安装git

cd ~/

尝试.ssh是否存在
如果不存在
mkdir .ssh
cd .ssh
ssh-keygen -t rsa -C "邮箱地址" 之后回车
用记事本打开id_rsa.pub     
notepad id_rsa.pub

如果存在，那么进去之后使用notepad 打开id_rsa.pub   
notepad id_rsa.pub

复制id_rsa.pub到
https://github.com/settings/keys  new ssh



